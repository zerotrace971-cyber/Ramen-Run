[package]
name = "ramen-vault"
version = "1.0.0"
edition.workspace = true
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk.workspace = true
stamp-shelf = { path = "../stamp-shelf" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
