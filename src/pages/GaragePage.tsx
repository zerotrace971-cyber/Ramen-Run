import { Check, LockKeyhole, Paintbrush, Wrench } from 'lucide-react';
import { useState } from 'react';
import { Link } from 'react-router-dom';
import { useGameProgress } from '../game/progress';

const decals = [{ id: 'miso', name: 'Miso Meteor', color: '#ffc95b' }, { id: 'neon', name: 'Neon Nori', color: '#5ae5e1' }, { id: 'rival', name: 'Mika’s Respect', color: '#ff5e9e', locked: true }];
export function GaragePage() { const [selected, setSelected] = useState('miso'); const { progress } = useGameProgress(); return <div className="garage-page"><header><p>SCOOTER CUSTOMIZATION</p><h1>Suzume’s garage</h1><span>Purely cosmetic. Extremely important.</span></header><section className="garage-layout"><article className="garage-scooter"><div className="garage-rider"><span className="garage-face" /><i>🚀</i></div><div className="garage-floor" /><p>ROCKET SCOOTER // MK. 04</p></article><article className="garage-panel"><h2><Paintbrush size={18} /> Route decals</h2>{decals.map(decal => <button key={decal.id} className={selected === decal.id ? 'selected' : ''} onClick={() => !decal.locked && setSelected(decal.id)}><i style={{ background: decal.color }} /> <span><b>{decal.name}</b><small>{decal.locked ? 'Complete Mika’s quest' : 'Unlocked'}</small></span>{decal.locked ? <LockKeyhole size={15} /> : selected === decal.id ? <Check size={17} /> : null}</button>)}<div className="garage-stats"><p><Wrench size={16} /> Tune-up effects</p><span>Boost sound: extra crunchy</span><span>Current coins: {progress.coins}</span></div><Link to="/game/map">Back to star map →</Link></article></section></div>; }
